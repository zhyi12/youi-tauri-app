<script>
    import {getContext} from "svelte";
    import {page} from "$app/stores";

    import {Form,FormItem,FieldInput} from "$lib/youi/index";

    let text = '';

    const {add{{cname}}} =  getContext('dialogContext');

</script>

<Form onSubmit={async ()=>{
    add{{cname}}({
        text,
    },$page.params.refId,$page.params.position);
}}>
    <FormItem caption="名称">
        <FieldInput notNull="true" bind:value={text}/>
    </FormItem>
</Form>